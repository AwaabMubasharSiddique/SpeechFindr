# Backend API base URL used by the Project page.
# Copy this file to ".env" (or ".env.local") and change the value to point at
# your backend. If unset, the app falls back to http://127.0.0.1:8001.
VITE_API_BASE=http://127.0.0.1:8001
